var Symbols;
(function (Symbols) {
})(Symbols || (Symbols = {}));
export function schema(strings, ...values) {
    const content = processTemplateStrings(strings, values);
    return {
        sql(strings, ...values) {
            return processTemplateStrings(strings, values);
        },
        __content: content,
        __type: null,
    };
}
function processTemplateStrings(strings, values) {
    let str = "";
    strings.forEach((string, i) => {
        str += string + (values[i] || "");
    });
    return str;
}
/*

const getTasks = MyApp.sql<{

}>`SELECT * FROM task WHERE owner_id = ?`;

CREATE TABLE task (
  id INTEGER PRIMARY KEY NOT NULL,
  what TEXT NOT NULL,
  owner_id INTEGER NOT NULL,
  complete BOOLEAN NOT NULL,
  list_id INTEGER
);
*/
//# sourceMappingURL=index.js.map