import { Changes } from "@vlcn.io/ws-common";
import { Transport } from "../transport/Transport";
import { DB } from "../DB";
/**
 * Represents a stream of changes coming into the local
 * database from one or more remote databases.
 */
export default class InboundStream {
    #private;
    constructor(db: DB, transport: Transport);
    prepare(lastSeens: [Uint8Array, [bigint, number]][]): void;
    receiveChanges: (msg: Changes) => Promise<void>;
}
//# sourceMappingURL=InboundStream.d.ts.map