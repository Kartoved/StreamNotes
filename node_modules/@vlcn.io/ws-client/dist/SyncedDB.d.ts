import type { Config } from "./config.js";
import { TransporOptions } from "./transport/Transport.js";
export interface ISyncedDB {
    start(): Promise<void>;
    stop(): boolean;
}
export declare function createSyncedDB(config: Config, dbname: string, transportOptions: TransporOptions): Promise<ISyncedDB>;
/**
 * Ensures that only one tab or worker is syncing the DB at a time.
 * As soon as a tab or worker dies, then the next tab or worker will
 * be able to sync the DB.
 * @param dbname
 * @param transportOptions
 */
export declare function createAndStartSyncedDB_Exclusive(config: Config, dbname: string, transportOptions: TransporOptions): Promise<{
    stop: () => void;
}>;
//# sourceMappingURL=SyncedDB.d.ts.map