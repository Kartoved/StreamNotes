import { Fragment as _Fragment, jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
// Just provide a DB.
// Do not do any sync. Sync can be configured separately.
// no wasm url.. that optimize deps thinger
import { useEffect, useState } from "react";
import dbFactory from "./DBFactory.js";
import { createContext } from "./DBContext.js";
export default function DBProvider({ dbname, schema, fallback, Render, manualSetup, }) {
    const [contextRef, _setContextRef] = useState(createContext);
    const [dbRef, setDbRef] = useState(null);
    useEffect(() => {
        let closed = false;
        dbFactory
            .get(dbname, schema, contextRef.useDb)
            .then((db) => {
            if (closed) {
                return;
            }
            if (manualSetup) {
                manualSetup(db)
                    .then(() => {
                    if (closed) {
                        return;
                    }
                    setDbRef(db);
                })
                    .catch((e) => {
                    console.error("Error running manual setup", e);
                    throw e;
                });
            }
            else {
                setDbRef(db);
            }
        })
            .catch((e) => {
            console.error("Error creating db", e);
            throw e;
        });
        return () => {
            closed = true;
            setDbRef(null);
            dbFactory.closeAndRemove(dbname);
        };
    }, [dbname, schema, contextRef.useDb]);
    if (dbRef === null || dbFactory.getHook(dbname) == null) {
        return _jsx(_Fragment, { children: fallback }) ?? _jsxs("div", { children: ["Creating DB ", dbname] });
    }
    return (_jsx(DbAvailable, { ctx: dbRef, DBContext: contextRef.DBContext, children: _jsx(Render, {}) }));
}
function DbAvailable({ children, ctx, DBContext, }) {
    return _jsx(DBContext.Provider, { value: ctx, children: children });
}
//# sourceMappingURL=DBProvider.js.map