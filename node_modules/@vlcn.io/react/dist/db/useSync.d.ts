export type Options = {
    dbname: string;
    endpoint: string;
    room: string;
    worker: Worker;
    authToken?: string;
};
export default function useSync({ worker, dbname, room, endpoint, authToken, }: Options): void;
//# sourceMappingURL=useSync.d.ts.map