import React from "react";
/**
 * Convenience function for mirroring a prop into state and ensuring that the state is updated
 * when the prop changes.
 *
 * Removes all the dragons from the pattern and gives us fine grained control over updates.
 *
 * @param propValue
 * @returns
 */
export declare function useCachedState<T>(propValue: T): [T, (value: T) => void];
export declare function useCachedBinding<T>(attr: keyof React.AllHTMLAttributes<T>, value: any, event: keyof React.DOMAttributes<T>, handler: React.ReactEventHandler<T>): {
    [x: string]: any;
};
//# sourceMappingURL=stateHooks.d.ts.map